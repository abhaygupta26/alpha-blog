<h1 align="center" class="text-white mt-4">Articles Listing</h1>
  <%= render 'article' %>
    <div class="flickr_pagination mb-2 mt-2">
        <%= will_paginate @articles, :container => false %>
    </div>